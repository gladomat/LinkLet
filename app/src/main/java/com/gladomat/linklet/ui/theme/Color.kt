package com.gladomat.linklet.ui.theme

import androidx.compose.ui.graphics.Color

// Accents — each palette promotes one of these to `primary`.
val AmberGold = Color(0xFFFFBF00)
val MagentaBloom = Color(0xFFE83F6F)
val RichCerulean = Color(0xFF2274A5)
val SeaGreen = Color(0xFF32936F)

// Neutrals shared by every dark scheme.
val Charcoal = Color(0xFF111111)
val MidnightSurface = Color(0xFF1C1C1C)

// Light backgrounds, tinted towards each palette's accent.
val SnowDrift = Color(0xFFFDFBF3)
val CeruleanMist = Color(0xFFF3F8FC)
val SeaGreenMist = Color(0xFFF2FAF6)
val MagentaMist = Color(0xFFFDF4F7)

// Light-scheme ink, dark enough for body text on the matching background.
val AmberInk = Color(0xFF1A1200)
val CeruleanInk = Color(0xFF0A1721)
val SeaGreenInk = Color(0xFF06180F)
val MagentaInk = Color(0xFF210610)

// Dark-scheme foreground paper, tinted towards each palette's accent.
val PaperSurface = Color(0xFFF7F4E9)
val CeruleanPaper = Color(0xFFE8F1F8)
val SeaGreenPaper = Color(0xFFE9F5F0)
val MagentaPaper = Color(0xFFF8ECF1)
